export interface ComponentConfig {
  token: string;
  company_domain: string;
  deal_note: string;
  owner_id: string;
  recordUid: number;
  applicationUid: string;
  domainId: string;
  schema: {};
}
